# Flask-REST-API-
**Flask Notes API** is a Python REST backend with full CRUD — create, read, update, and delete notes via HTTP. Supports tag filtering, full-text search, and clean JSON responses with proper status codes. A solid, ready-to-extend foundation for any frontend or mobile app.
